import React from "react";

const OperatorButton = props => {
  return (
    <button className="operator_button general_button">
      {props.button}
    </button>
  );
};

export default OperatorButton;
